{
  "rules": {
    "presence": {
      ".read": "auth != null",
      "$uid": {
        ".write": "$uid === auth.uid",
        ".validate": "newData.hasChildren(['username', 'state', 'lastActive'])",
        "username": { ".validate": "newData.isString() && newData.val().length <= 30" },
        "state": { ".validate": "newData.isString() && (newData.val() === 'online' || newData.val() === 'idle' || newData.val() === 'offline')" },
        "lastActive": { ".validate": "newData.isNumber()" },
        "currentPath": { ".validate": "newData.isString()" }
      }
    },
    "status": {
      ".read": "auth != null",
      ".write": false
    },
    "typing": {
      "$roomId": {
        "$uid": {
          ".read": true,
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}
